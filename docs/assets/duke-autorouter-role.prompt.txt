Make one precise text edit to this DUKE Autorouter architecture graphic. On the purple dashed connection near the upper middle, replace the label 'Observation mode' with 'Task classification + confidence'. This label describes Jev's role and output, not its temporary rollout setting. Keep the smaller line 'Brief + tool needs only' beneath it. Adjust only that label's font size or available width as needed for clean, readable spacing; do not overlap nearby text, cards, or connectors. Preserve every other pixel-level feature as closely as possible: all cards and arrows, the title DUKE Autorouter, acronym expansion and tagline, footer legend, and the pending live-validation note. The API-limits block has already been removed and MUST stay absent. Do not add dollar amounts or restore that block. Maintain 1536x1024 landscape dimensions and the existing colors.
