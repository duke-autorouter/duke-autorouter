Edit this existing technical architecture diagram, preserving its entire layout, every card, every connector, every icon, the local/remote boundary, all non-header text, and its precise factual meaning. Make only this branding update at the top: replace the main title 'Duke Router' with the exact complete product name 'DUKE Autorouter'. Replace the subtitle 'Local task execution across subscription and API models' with the exact acronym expansion 'Decides Using Knowledge and Evidence'. Add the small crisp tagline 'Give it the task. It chooses the model.' if there is room in the header without moving any diagram elements; omit that tagline rather than cluttering the header. Keep the architecture version badge and all remaining labels exactly unchanged, especially 'Observation mode', 'Optional task classification', both subscription providers, OpenRouter paid API models, $5/day and $25/month, and the bottom note that live provider and routing-quality validation is pending. Maintain the 1536x1024 landscape aspect ratio and the ivory/forest green/sage/lavender/ochre palette. Crisp, dark, highly legible header typography, no new decorative elements, no invented connections. This is a branding edit, not a redesign.
