# Jev judgment maintenance checks

September 23, 2026. Candidate 0.1.10.

A split between routine and standard difficulty previously stopped Jev before model selection. DUKE now chooses the lowest difficulty whose conservative cumulative probability reaches 0.80, otherwise complex. Displayed rounding is accounted for. This is an ordinal policy bound, not a calibrated success estimate. Task type still needs a chosen-option probability of 0.80; incomplete context forces complex. Invalid answers and outages retain the configured economical fallback.

Semicolon-delimited review requirements now receive separate questions. Quoted code and bracketed expressions retain their semicolons. Each clause is interpreted with the full request, including conditions and exceptions. Review overflow stays unverified. Verdict thresholds, correction limits, escalation gates, and verifier trust boundaries are unchanged. Routing/review policy versions changed so older judgments do not enter the new evidence scopes.

## Evidence

All 248 local tests passed, with zero failures or skips, using macOS native helpers. TypeScript passed. Added tests cover ordinal distributions, the rounding boundary, separate clause verdicts, and overflow remaining unverified.

Four invented review probes were frozen at `143d4ee` before calls. A supported club update passed, a wrong meeting date failed, an invented commitment failed, and a withheld source remained unverified. The [receipt](evidence/judgment-boundaries-20260923.json) retains every verdict. Actual Jev spending: $0.000624.

Two fresh routing briefs used three explicit development profiles: GPT-6 Luna, Sol, and Astra, each with Low/Medium options. Jev selected Luna Low for both sentence-case labels and a bounded grouping function. The coding difficulty distribution split routine/standard, and still reached model selection. No worker executed these briefs; this checks the selection path, not the chosen model's success. See the [routing receipt](evidence/judgment-routing-20260923.json). Actual Jev spending: $0.000292.

The first routing harness attempt failed local profile-schema validation before any API request. The corrected profile schema was committed at `3bb9441` before retrying the unchanged briefs. Its original one-cent reservation remains conservative in the private budget ledger; no provider spend occurred on that failed startup. All completed runs have zero unreconciled requests and remain inside the existing shared $1 authorization.

## Limits

These six probes are a small development sample, not held-out accuracy or efficiency evidence. They do not establish that the earlier six-task review incompleteness is resolved. Clause context still depends on Jev's interpretation. More varied coding, documents, conditional requirements, and incomplete-source cases remain useful follow-up work. Passing arbitrary worker-authored tests cannot certify semantic requirements.
