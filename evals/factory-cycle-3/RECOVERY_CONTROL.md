# Separately frozen word-count recovery control

This is not a repeat or replacement of cycle 3. Before live execution, freeze one invented Hazel workshop brief and a 90–110-word requirement through `run-gpt6.ts --word-count-recovery --mode probe`. A deliberately short first file is injected, with no initial worker inference, at a forced Luna Low route. Then the real engine verifies it and decides recovery under unchanged limits. Any subsequent worker invocation is real. This establishes only controlled-path evidence; it does not estimate natural failure, success or efficiency rates.

Use a fresh state/output and the existing shared budget gate with a $0.03 reservation, after checking weekly usage remains below50% used. Run once; retain failure or uncertainty. Independently inspect length/facts and record actual model/effort, recovery decisions and all costs. Preserve source hashes. No forced recovery judgment, threshold tuning or premium promotion.
