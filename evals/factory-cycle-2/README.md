# Factory cycle 2: fresh review and preview checks

Frozen September 23, 2026, before live calls, against source `c5c3c00` (unreleased parser correction). Seven invented Jev-only probes cover two supported ownership formats, an owner transferred from another item, an invented owner, withheld source, fourteen numbered requirements, and a wrong fourteenth field. Expected outcomes are fixed in `fixtures.ts`. This is a small diagnostic sample, not calibrated accuracy, worker success or efficiency evidence.

Run `scripts/evals/check-factory-review.ts --run --out NEW_EMPTY_PATH` under the existing shared live-budget gate, reserving $0.02 from the unchanged combined $1 authorization. The isolated state also caps Jev at $0.02. No Codex/Claude worker runs are involved. Missing `--run` prints the frozen case inventory without provider calls. Each review has a 90-second limit; partial receipts and spending survive errors. No model or threshold changes may be made to force these cases to pass, and any repeat must remain labeled a repeat.

The separate local preview investigation uses invented documents and the existing native helper. It must preserve the first-page/thumbnail limitation and distinguish a rendering defect from incorrect document contents. No new package or public release is part of this cycle. Weekly Codex reserve remains 50%; starting snapshot is 40% used. The scheduled pilot remains paused.

## Explicit-scope control pair

After preserving the seven initial results, two additional cases were frozen before their own calls. Both original supported ownership checks passed; only broad completion stayed uncertain. The original prompt requested a handoff without explicitly limiting it to the output's one item. The new pair requests exactly one line about one named item and explicitly excludes the other source item. It tests a scope-ambiguity hypothesis using new names and facts, not a changed threshold. Run with `--scope-controls`, a new empty output directory, and a separate $0.01 reservation under the unchanged shared cap. Never replace the initial results with these controls.
